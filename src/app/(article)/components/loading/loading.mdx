import { Grid, Loading } from '@/packages';
import Playground from '@/src/app/components/playground';

# Loading

Indicate an action running in the background.

## Import

```tsx
import { Loading } from 'raw-ui';
```

## Types

<Playground
  scope={{ Grid, Loading }}
  code={`
<Grid gutter={[32, 0]} align="center">
  <Grid.Col>
    <Loading />
  </Grid.Col>
  <Grid.Col>
    <Loading type="spin" />
  </Grid.Col>
</Grid>
`}
/>

## Sizes

<Playground
  scope={{ Grid, Loading }}
  code={`
<Grid gutter={[0, 32]}>
  <Grid.Col span={24}>
    <Grid gutter={[32, 0]} align="center">
      <Grid.Col>
        <Loading size={4} />
      </Grid.Col>
      <Grid.Col>
        <Loading size={6} />
      </Grid.Col>
      <Grid.Col>
        <Loading size={8} />
      </Grid.Col>
    </Grid>
  </Grid.Col>
  <Grid.Col span={24}>
    <Grid gutter={[32, 0]} align="center">
      <Grid.Col>
        <Loading type="spin" size={16} />
      </Grid.Col>
      <Grid.Col>
        <Loading type="spin" size={20} />
      </Grid.Col>
      <Grid.Col>
        <Loading type="spin" size={32} />
      </Grid.Col>
    </Grid>
  </Grid.Col>
</Grid>
`}
/>

## With text

<Playground
  scope={{ Grid, Loading }}
  code={`
<Grid gutter={[32, 0]} align="center">
  <Grid.Col>
    <Loading>Loading</Loading>
  </Grid.Col>
  <Grid.Col>
    <Loading type="spin">Loading</Loading>
  </Grid.Col>
</Grid>
`}
/>

## API

| Prop | Description  | Type                 | Default                                |
| ---- | ------------ | -------------------- | -------------------------------------- |
| type | loading type | 'spin' \| 'dot'      | 'dot'                                  |
| size | input size   | number               | 4 (type = 'dot') \| 20 (type = 'spin') |
| ...  | native props | React.HTMLAttributes | -                                      |
